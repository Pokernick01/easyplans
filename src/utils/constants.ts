// ---------------------------------------------------------------------------
// Default element dimensions (meters)
// ---------------------------------------------------------------------------

/** Default wall thickness in meters. */
export const DEFAULT_WALL_THICKNESS = 0.15;

/** Default wall height in meters. */
export const DEFAULT_WALL_HEIGHT = 2.8;

/** Default floor-to-floor height in meters. */
export const DEFAULT_FLOOR_HEIGHT = 3.0;

/** Default door width in meters. */
export const DEFAULT_DOOR_WIDTH = 0.9;

/** Default door height in meters. */
export const DEFAULT_DOOR_HEIGHT = 2.1;

/** Default window width in meters. */
export const DEFAULT_WINDOW_WIDTH = 1.2;

/** Default window height in meters. */
export const DEFAULT_WINDOW_HEIGHT = 1.2;

/** Default window sill height from floor in meters. */
export const DEFAULT_WINDOW_SILL = 0.9;

// ---------------------------------------------------------------------------
// Grid (meters)
// ---------------------------------------------------------------------------

/** Major grid spacing in meters. */
export const GRID_SIZE_MAJOR = 1.0;

/** Minor grid spacing in meters. */
export const GRID_SIZE_MINOR = 0.1;

// ---------------------------------------------------------------------------
// Interaction thresholds
// ---------------------------------------------------------------------------

/** Snap distance threshold in screen pixels. */
export const SNAP_THRESHOLD = 10;

/** Minimum wall length in meters. */
export const MIN_WALL_LENGTH = 0.1;

/** Minimum room area in square meters. */
export const MIN_ROOM_AREA = 0.5;
